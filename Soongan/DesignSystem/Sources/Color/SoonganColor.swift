//
//  SoonganColor.swift
//  Studing
//
//  Created by ParkJunHyuk on 5/1/25.
//

import SwiftUI
import Resource

public extension Color {
    static let black100 = Color(asset: ResourceAsset.Color.black100)
    static let black80 = Color(asset: ResourceAsset.Color.black80)
    static let black60 = Color(asset: ResourceAsset.Color.black60)
    static let black40 = Color(asset: ResourceAsset.Color.black40)
    static let black20 = Color(asset: ResourceAsset.Color.black20)
    static let error = Color(asset: ResourceAsset.Color.error)
    static let primary = Color(asset: ResourceAsset.Color.primary)
    static let secondary = Color(asset: ResourceAsset.Color.secondary)
    static let backgroundGray = Color(asset: ResourceAsset.Color.bgGray)
    static let modalBackground = Color(asset: ResourceAsset.Color.bgModal)
}
